#include <bits/stdc++.h>
#define FIO ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0)
using namespace std;
char a;
void IO() {
    FIO;
    cin >> a;
}

int main() {
    IO();
    printf("%d", a);
    return 0;
}